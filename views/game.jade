#player1.name
  | GeorgeWashington
table#chessboard(cellpadding=0, cellspacing=0)
#player2.name
  | AbrahamLincoln (you)

h2#turn
#table_info
  | Pick a color:
  input(type="submit", value="white", id="pick_white")
  input(type="submit", value="black", id="pick_black")

:javascript
  | var game_id = "#{game_id}";
  | var board = new Chessboard(!{game_state});
  | var your_color = "#{your_color}";
