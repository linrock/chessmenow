#tablearea
  #boardarea
    #chessboard
    button#choose-white.choose White
    button#choose-black.choose Black
    // #turn
  #infoarea
    #top-side.color-info
      #top-name
        .black-player
      .clear
      #top-captured
    #bottom-side.color-info
      #bottom-captured
      .clear
      #bottom-name
        .white-player
.clear
#announcement-area
  h2#info

script(type="text/javascript", src="/media/js/chessboard.js")
:javascript
  | var game_id = "#{game_id}";
  | var player_state = !{player_state};
  | var player = new Player({ pid: player_state.id });
  | var board = new Chessboard(!{game_state}, !{player_state});


  | console.dir('dude, sending info through socket...');
  | player.get('socket').send({ type: 'colors', game_id: game_id, player_id: 'asdfdude', color: 'b' });
